//when we default a export in that case we can change their name i.e there can only be one default prop export
const a = 9;
export default a;

//when we export a prop without default then we cannot change the name as we are specifically using the propery

export const b =0;